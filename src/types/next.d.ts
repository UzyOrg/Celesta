/// <reference types="next" />
/// <reference types="next/image-types/global" />

/**
 * Keep Next's ambient CSS-module declarations available to standalone
 * `tsc` runs. `next-env.d.ts` is generated locally and intentionally ignored,
 * while CI runs typecheck before a Next build can regenerate it.
 */
